# Build Strata Installer (ChatGPT handoff)

- Task ID: `6a918f65-62b0-83e9-8901-3bcfc81134f3`
- Source: ChatGPT conversation
- Scope: user-visible messages returned by the accessible task history

> Historical context only. The current user request, repository `AGENTS.md`, and current source are authoritative.

## User

Build Strata into an installer package and upload it as a GitHub release. Include .NET where practical, apply the required Windows and shell settings, include the default Strata wallpapers, and offer to set Strata as the default shell and launch it after installation.

Use the current settings as defaults only after removing identifiable or private values such as weather location, passwords, search history, and logins. Add a polished Strata Launcher for Explorer mode that can select Strata as the shell, update it, and reset settings. Register a full uninstaller that restores Explorer and Windows settings and lets the user choose whether to retain Strata settings.

## Assistant

The installer task was handed to a Codex work session with the privacy scrub, bundled runtime, launcher utility, shell switching, wallpapers, uninstall rollback, Add/Remove Programs registration, and GitHub Release scope preserved.
